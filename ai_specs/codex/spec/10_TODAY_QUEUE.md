# Today Queue — Not Implemented

JobTrackerr! has no Today route, next-action fields, deadlines, overdue calculation, stale threshold, scheduler, or notification service.

The only application date is `applied_at`, plus server timestamps. Notes may hold informal reminders but are not parsed or scheduled. A follow-up queue is roadmap work and must not be assumed without an approved model and behavior spec.
