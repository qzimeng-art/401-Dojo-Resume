# Team Work Areas

Natural ownership areas are Django API/models/tests; web auth/public pages; web dashboard/files/reviews; mobile auth/navigation; mobile dashboard/application flows; deployment; and docs/QA/accessibility.

Shared contracts are application field names, exact statuses, auth token behavior, API URLs, and owner isolation. Coordinate these before merging. Avoid parallel implementations of fictional ApplyFlow modules.
