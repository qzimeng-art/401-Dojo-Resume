# Notes and History

Applications include notes, requirements, files, and created/updated timestamps. These provide context but do not form an event timeline.

There is no recruiter/contact model, inbound/outbound message, response type, status history, or activity feed. Status edits overwrite the current value. Communication history is roadmap-only; use notes for user-entered context and do not claim automatic tracking.
