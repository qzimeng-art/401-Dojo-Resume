# Codex Project Prompt

Work on the existing **JobTrackerr!** application in the `401-Dojo-Resume` repository.

Before editing, read `spec/00_MASTER_SPEC.md` and the topic-specific spec. Treat the implementation and database migrations as the source of truth when prose is stale. Preserve React/Vite web, Expo mobile, and Django REST architecture.

Do not invent ApplyFlow branding, Next.js/Supabase infrastructure, structured resume editing, tailored resumes, match scoring, a Today queue, or a communication timeline. The implemented resume feature is document upload/display only. Preserve data isolation and explicitly account for the current shared demo-user behavior, then run relevant tests/builds.
